# Anomaly Hand

A portrait-driven short-run card battler for mobile. Read the enemy's next intent, play one of three tactical cards, build sequence, and deploy each operative's signature order across three connected encounters.

## Development

```bash
npm install
npm run dev
npm run build
```

The production build uses portable relative asset paths and is emitted to `dist/`.
